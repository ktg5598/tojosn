
这次笔试没有使用docker,因为没有解除过这个，在写完代码的时候尝试去百度做一下，但是安装之后启动不了。
因此只用python写了一下转json：
	使用的是xmltodict包完成的转码，将git上的xml文件下载到本地了，也可以直接克隆到本地，遍历文件目录获取前24小时的所有xml文件，
          然后使用文本打开读取其中的内容，在使用xmltodict转换，再经过json转换就完成了格式转换。